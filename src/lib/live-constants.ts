/** Minutes after first live score when wrap-up UI is shown. */
export const LIVE_WRAPUP_AFTER_MIN = 90;
/** Minutes after first live score when a submission is auto-created if none submitted. */
export const LIVE_AUTO_SUBMIT_AFTER_MIN = 100;
